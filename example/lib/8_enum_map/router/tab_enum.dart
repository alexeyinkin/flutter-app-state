import 'package:enum_map/annotations.dart';

// If this shows error, run `flutter pub run build_runner build`.
// In Android Studio, you may also need to close and re-open the project
// for the generated file to be recognized.
part 'tab_enum.g.dart';

@unmodifiableEnumMap
enum TabEnum {
  books,
  about,
}
